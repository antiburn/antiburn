# Deliberate Deviations

Every place the shipped application knowingly differs from the ratified feature
matrix, with the reason and the milestone it should be revisited at.

This file exists because the alternative is worse. A deviation that lives only
in a commit message is indistinguishable, six months later, from a thing
somebody forgot to build — and the difference between those two is the whole
value of having a ratified matrix. If a row below stops being true, either the
code or this file is wrong, and both are cheap to check.

**What belongs here:** a difference from an approved **Keep**, **Localize**, or
**Include** row that was decided rather than missed.

**What does not:** anything the matrix marks **Private only**, **Omit**, or
**Defer**. Those are absences by design, not deviations — the private
application's authentication, organizations, publication, and commercial
enrichment features have no public counterpart and no public placeholder, which
is the matrix working as written.

Related: the conformance audit (kept with the project's private governance
records), the support and
data-handling claims in [`docs/support.md`](support.md).

## Register

| # | Deviation | What the matrix asks for | What ships | Why | Revisit at |
| --- | --- | --- | --- | --- | --- |
| D-1 | **About carries no external links** | About links to release notes, licence, source, security, support, and privacy | The licence *stated* (MPL-2.0, with the full text and the legal notices readable in About itself, and a header in every source file) and a row that opens the Privacy pane | This repository is not public yet, so every one of those links would open a browser at a 404. In the one pane whose job is to establish provenance, a dead link is worse than an honest silence. | Publication. The links land the day the repository is public. |
| D-2 | **No Send Feedback pane** | Settings → Send Feedback opens a public issue, discussion, or support URL in the browser | Nothing. No pane, no disabled row. | Same cause as D-1: there is no public issue tracker to open yet. Under the omission discipline a control that cannot work is not shipped disabled — it is not shipped. | Publication, with D-1. |
| D-3 | **No Debug pane** | Settings → Debug with debug-build simulations of public states and routes | Nothing | The omission discipline again, applied to ourselves: the pane's value is deterministic UI states for contributors, and there are no outside contributors until the repository is public. Building a contributor affordance before there are contributors is how placeholder surfaces get shipped. | Publication, if a contributor workflow needs it. |
| D-4 | **No update download, install, restart, progress, or post-update interstitial** | Update progress (check/download/install/failure/retry/restart) is **Keep**; the updated interstitial and release notes are **Localize** | A check, its result, and a schedule preference. `UpdatesPane` reports what the last check found; the notification body says in as many words that this build does not install updates. | The install flow has not been built. The original blocker is gone — the updater key was minted 2026-08-14, `plugins.updater.pubkey` carries the public half, and releases produce signed bundles the app can verify — so this deviation is now UX debt rather than a security refusal: the check exists, the install/progress/restart flow does not yet. | The revisit condition (the key pair existing) arrived 2026-08-14; the flow is unblocked and should be built before the first supported release. The copy in `notifications::update_message` and `UpdatesPane` is written to be revisited now. |
| D-5 | **Onboarding Welcome promises no analytics opt-in** | Welcome copy mentions that anonymous analytics is sent only after a separate, default-off opt-in | Copy that says no usage data is collected at all | This build ships no analytics of any kind, so promising the reader a control over analytics would be describing an app nobody has written. Shipping neither the analytics nor the promise is the only internally consistent pair. The rationale is also stated at the top of `OnboardingFlow.tsx`. | The next governance review. Either the matrix row is amended or the analytics channel is built and consented for; the current state needs the same amendment either way. |
| D-6 | **Resolved (2026-08-14): the floating notification window ships** | A floating notification window, retained as a generic mechanism | The matrix row as written: a slim always-on-top notification window (ported from the private app under D-023), with placement, auto-dismiss, sound, and a test button in Settings → Notifications | The original deviation reasoned that the platform's centre already did this better; live use disagreed — the centre buries short local messages, offers no placement, and cannot preview. The revisit condition ("presentation the platform cannot express") was met, and the port was authorized rather than re-implemented. The window is mechanism-only; every gating decision stays in `notifications.rs`. One trade-off is accepted knowingly: the slim port consults no Focus/Do-Not-Disturb state, so a nudge can appear during Focus where an OS notification would have been held. | If Focus-awareness is ever wanted, it returns as its own reviewed port. |
| D-7 | **Seven notification kinds, two of them usage-aware** | Approved local scan, source-error, update, and evidence-backed Provider Usage notifications | An available update, a failed scan, low disk space, a spend anomaly, a usage milestone, where the app went when the first run finished (D-26), and the settings pane's own test | Source-access problems remain a banner (see D-8). The usage kinds thread the needle the old row refused to fudge: the **anomaly** compares the machine against its own history (last hour ≥ a quarter of the trailing week, over a floor) and its copy names the figures as local estimates; the **milestone** engine fires only on a provider's own authoritative percentages, which the Usage surface now has. Milestones are gated on `live_usage_enabled` — the Settings → Usage switch, default off — and that gate is deliberate rather than incidental: milestones need readings that keep *moving*, and only the refresh source makes them move. A reader who never turns the switch on sees limits but is never interrupted about them. The thresholds themselves stay default-on, so the switch is the single consent point, and its copy names both of its consequences. Nothing invents a threshold it cannot know. | The anomaly rule revisits if real use shows it noisy or mute; the milestone rule revisits if the single-switch coupling proves confusing. |
| D-8 | **Two attention banners, not four** | Source access problems, repeated scan failures, storage/migration problems, and update availability | Source access and storage | The other two already have a home that is more useful than a banner: a failed or repeated-failure scan is reported in the status line directly above the list (with the controls to retry it), and update availability is in the Updates pane and, once, in a notification. Three surfaces for one fact is nagging. | Only if the status line proves too quiet in live use. |
| D-9 | **Storage *open* failures are fatal, not banner-surfaced** | Attention banners cover storage problems | Write failures raise a banner with a retry; a database that cannot be opened at all stops the app at launch | There is no useful antiburn without its store: every surface reads from it. An app that starts into a permanent error banner, with an empty list and no working control, is a worse answer than one that refuses to start. Write failures are different — they are usually transient (a full disk, a locked file) and the retry is an ordinary rescan. | Only if a recoverable open failure turns up in practice. |
| D-10 | **Quit does not confirm** | "Preserve native confirmation behavior" for the quit action | Both quit paths — the tray menu and the Settings sidebar — exit immediately | Nothing in this application is unsaved: every setting writes through on change, and the index is rebuilt by the next scan. A confirmation would be asking a reader to approve the loss of nothing. The two paths behave identically on purpose. | Only if quitting ever becomes destructive. |
| D-11 | **Resolved (2026-08-18): launch at login is enforced** | General carries the everyday controls | The first-run Ready screen asks explicitly, default on, and General reflects the same preference. Distribution builds reconcile the operating-system registration when onboarding finishes, whenever the setting changes, and at later launches. | macOS 13+ uses `SMAppService.mainApp`, including the system's requires-approval state; Windows writes a quoted per-user Run command; Linux writes an escaped Desktop Entry. Registrations are refreshed to the current executable path. A dedicated Cargo feature limits mutation to distribution builds, so even `cargo run --release` is inert. Existing completed installs retain their stored choice, while a new install sees the default before anything is registered. | Revisit if a supported platform changes its startup API or if macOS approval recovery needs a direct System Settings affordance. |
| D-12 | **`desktop-frontend` CI omits macOS** | "Frontend and Tauri checks pass on the supported release matrix" | Linux and Windows for the frontend suite; Linux, Windows, and macOS for the shell | The frontend suite is jsdom. Windows earns its place — path handling, line endings, and the platform-aware modifier logic are exactly what a Linux-only run cannot fail — while macOS shares its behaviour with Linux there and is the slowest runner. The revisit clause has fired once: the settings window now renders macOS-only chrome (the title-bar drag strip and its clearances, branched on `isMacOS()`). It does not change the answer, because jsdom has no operating system — those tests run under a mocked `isMacOS` and are bit-identical on every runner, so a macOS jsdom run would re-execute the mock, not the platform. The code that genuinely differs per OS (tray, anchoring, window policy, the overlay title bar itself) is in the shell, which does run on all three, and the chrome has a manual check besides. The rationale is also written into `.github/workflows/ci.yml`. | Whenever frontend behaviour starts depending on the *real* host — a native webview API jsdom cannot fake — rather than on a mocked platform answer. |
| D-13 | **Resolved (2026-08-17): one required check fronts a semantic CI matrix** | Not a matrix row; a gap recorded here so it is not rediscovered | `ci-required` is the stable branch-protection check, frontend Prettier runs on Linux, and a fail-closed classifier selects the affected platform jobs | The old workflow made every documentation or release-metadata PR wait for nine mostly unrelated jobs and repeated lint, types, tests, and build on Windows. The aggregate now stays present while documentation, frontend, engine, desktop-backend, and semantic release-only changes take their warranted routes. A purported release-only change is narrow only when structured comparison proves the manifests and lockfiles changed package-version fields alone; anything unknown or mixed expands to full CI. Rust dependency caches are readable by PRs and tags but writable only by trusted main pushes. A pure app version bump warms four unsigned, unbundled release targets on main; the release jobs restore those caches but cannot save after signing secrets enter scope. Finally, releases trust only the successful main push run for the exact tag SHA, so they preserve the integration gate without paying for the identical matrix twice. | Revisit if a classifier miss escapes, cache use approaches 8 GB, or 20 warm runs do not materially improve the recorded CI/release medians. |
| D-14 | **Published support claims are narrower than the matrix permits** | D-007 permits Windows 10 1809+ | `docs/support.md` declares Windows 10 "not tested; no support claimed" | Erring narrow is the correct direction for a support claim: the matrix says what may be supported, and the documentation should say what has been tested. This is a deviation in the safe direction and is recorded so it is not mistaken for an oversight. | Whenever Windows 10 is actually tested. |
| D-15 | **No Sound lab, though a sound now ships** | "Keep debug-only only if local notifications ship; otherwise omit" | The notification chime (ported synthesis crate under D-023) with a single on/off preference — and still no Sound lab | The lab's value is tuning knobs, and antiburn's chime is deliberately fixed: the tuned constants are ported verbatim from the reviewed source, a maintainer decision, not a per-user surface. A debug pane for parameters nobody may change would be scaffolding. | If the chime ever becomes user-tunable. |
| D-16 | **No platform-signed release can be built yet: the certificates do not exist** | Signed, notarized builds on the v1 target matrix, published as a reviewed draft release | A complete release pipeline (`release-app.yml`, `release-engine.yml`) that **fails loudly** rather than producing anything: it stops before building if `plugins.updater.pubkey` is empty, and each platform build stops unless its signing credentials are present. | The updater key pair was minted 2026-08-14 (public half committed, private half in the `release` environment), but there is still no Apple Developer ID certificate or Authenticode certificate to sign with — those need accounts and money, not code. The alternative to failing was faking: a build that skips signing and looks like a release. Refusing is the only honest state while the material is missing, and it keeps the pipeline reviewable now rather than written the day it is first needed. Two deliberate escape hatches exist for *platform* signing — the repository variables `ALLOW_UNSIGNED_MACOS` and `ALLOW_UNSIGNED_WINDOWS`, which produce an unsigned build that says so (the macOS hatch seals the bundle with codesign's ad-hoc identity, so Gatekeeper warns rather than reporting the app damaged). The **updater** signature has no escape hatch: without it `latest.json` would advertise downloads nothing can verify. | When the certificates and the key exist. Delete the two `ALLOW_UNSIGNED_*` variables that day. |
| D-19 | **The `release` environment has no required-reviewer gate** | Signing-material secrets gated on a human approving each release run (`docs/runbooks/release.md` §1) | The `release` environment holds `TAURI_SIGNING_PRIVATE_KEY(_PASSWORD)` with **no protection rules**: any run that references the environment gets the secrets without approval. | GitHub rejects environment protection rules on this repository: the feature is plan-gated for private repositories. The compensating controls today are a single-writer repository and the tag rulesets, which restrict who can start a release run at all. | When the repository becomes public (the feature unlocks, like attestation persistence) or the plan changes: add the required reviewer to the `release` environment that day, before the next release run. |
| D-17 | **Linux packages carry no platform code signature** | "Signed builds per target" | The AppImage and the `.deb` are unsigned as packages; they carry the updater's detached signature, a published SHA-256, and a build-provenance attestation | There is no Linux equivalent of Developer ID or Authenticode that antiburn could offer from a public runner: `.deb` signing means a repository and a key readers would have to trust separately, and AppImage signatures are verified by almost nothing in practice. Three verifiable facts about the bytes are worth more than a fourth signature nobody checks. | If antiburn ever publishes an apt repository, where a repository key is the mechanism people actually use. |
| D-18 | **Agent rows and provider glyphs show vendor brand marks from CC0 icon packages, not the private artwork** | The private app ships its own per-agent icon SVGs (denied source) | Brand marks from the CC0-licensed `simple-icons` package for Claude, Cursor, GitHub Copilot, Cline, OpenCode, Windsurf, and Pi, and OpenAI's mark for Codex from Gil Barbara's SVG Logos (CC0-1.0, inlined with its provenance in `lib/brandMarks.ts` because the collection is too large to depend on for one icon) — each verified against the icon's recorded brand source; letter tiles for Kiro, Antigravity, and Amp (the `simple-icons` `AMP` icon is Google's web framework, a name collision, so Amp deliberately gets a tile); surface glyphs for unknown agents. Marks render in a theme brand-mark ink (`--color-agent-mark`) except Claude, which renders in the clay orange its package records. The same treatment covers the **provider** glyphs on the usage surfaces (`ProviderUsagePrimitives.tsx`), under the same collision rule, and drawing each vendor with the mark its **session rows** already use so one vendor never looks like two: Anthropic is the Claude mark rather than the corporate one, OpenAI is the same mark Codex rows carry, and GitHub Copilot, Google Gemini, Cursor, Windsurf, OpenRouter, DeepSeek, and Mistral carry their own, while **xAI deliberately keeps a letter** — `siX` is the social network, not the model provider, and wearing it would be the Amp mistake again. | The private icon files are denied and vendor logos are the vendors' marks. Both packages provide rights-cleared (CC0) path data. Colour follows the same evidence rule as the artwork: a brand hex is used only where the colour carries the identity, and only as the value the package records — never one chosen here — while the rest stay ink so the set reads as one set and no near-black or near-white vendor hex disappears into a theme. Use is nominative — identifying which vendor's agent produced a session, or whose allowance a figure describes. | If a vendor objects, its entry drops back to a letter tile; if original artwork is commissioned, the `data-agent-icon` and `data-provider-icon` seams swap it in without touching call sites. Adopting the private app's own icon files was considered and declined: they carry no recorded source or licence, so they fail the boundary's asset-provenance rule regardless of the path rule, and the CC0 packages cover the same marks with evidence. Kiro, Amp, and xAI revisit if a rights-cleared mark for them appears. |

| D-20 | **Live plan limits are fetched directly from each provider, on by default, with one opt-out switch** | (Not a matrix row; recorded so the privacy claims stay auditable) | The Usage surface shows a provider's own plan-limit figures from a direct fetch. antiburn also saves the last complete display snapshot so it can paint that surface before a new request completes. For Claude, the fetch calls the provider's usage endpoint with the OAuth credential the Claude CLI already keeps on this machine — on macOS, that credential usually lives only in the login Keychain (a `security find-generic-password` read, the same item the CLI itself reads and writes), with `~/.claude/.credentials.json` (or `$CLAUDE_CONFIG_DIR`) as the fallback carrier there and the only carrier on every other platform. For Codex, the same idea uses ChatGPT's usage endpoint and the credential in `~/.codex/auth.json` (or `$CODEX_HOME`), refreshed once in-process if it has expired. When that direct call cannot get through, antiburn asks the same question over the local `codex app-server` process's JSON-RPC protocol, so it inherits the session that process already has working. All three ways of answering run by default once first-run setup is complete. A single switch in Settings → Usage turns every one of them off and hides the saved snapshot. No source runs, and no credential is read, until onboarding finishes. The first credential read this feature makes, including a possible macOS Keychain prompt, therefore occurs after the reader has seen what antiburn is. Every source is local by antiburn's definition (D-27): each one is antiburn acting as the reader, with the reader's own credentials, and none needs a service of ours. | The saved snapshot answers only "what did antiburn last see". The surface labels its age and marks it stale after a restart, while every open still asks "what is true right now" through the direct fetch or app-server fallback. The snapshot is a presentation bootstrap, not a substitute source: it prevents a blank surface and a late layout change without preventing or delaying a current request. Fetching the reader's own usage, from a provider they already use, with a credential they already hold, is an ordinary operation, not a risky one. antiburn runs it by default instead of gating it behind ceremony. The switch exists as an opt-out because "no background traffic" is a legitimate preference, such as on a corporate or metered network, or by plain choice. | If a provider's usage endpoint or its CLI's app-server protocol changes shape, the parser for that source fails closed (D-21) rather than guessing, and this is the row to revisit alongside it. Revisit the saved snapshot if retaining quota data creates a privacy concern. |

| D-21 | **A live reading is only as current as the moment it was taken, and the surface always says so** | (Not a matrix row; recorded so the privacy claims stay auditable) | Every plan-limit figure on the Usage surface carries the time it was read, and its copy names that time rather than implying "now". A reading is as old as the last successful direct fetch — each source retries on its own cooldown (success or failure) and keeps reporting its last good reading, restamped for how old it now is, rather than going blank between attempts or between app restarts. Past an hour, a reading is marked stale rather than left looking current. The pace and runway estimates degrade to "unavailable" rather than guess when the history behind them is too sparse or too stale to trust. | A limit figure that looks live but is hours old is worse than one honestly labelled, because a reader pacing against it would misjudge exactly when it matters most. The freshness label is therefore not decoration — it is the difference between a figure a reader can act on and one they cannot. This holds for whichever source produced the figure, which is why the sources described in D-20 never blend into one undated number. | If a provider ever exposes a genuinely streaming reading, "as of" stops being the right frame for that provider and this is the row to revisit. |

| D-22 | **The popover may exceed the contract's 700px, for one surface** | A 380px-wide, non-resizable popover whose per-view heights are bounded at 700px | Two clamps instead of one: `popover::DEFAULT_HEIGHT` stays at the contract's 700 — the size the window is created at, rests at, and gives the activity and session surfaces — while `popover::MAX_HEIGHT` rises to 780, which **only the Usage surface requests**. | The Usage surface outgrew the number. It was sized when a provider card was three spend windows and a token split; a card now carries the provider's own limits above that — three limit rows, three reset lines, and the pace block — and two vendors no longer fit. The alternative was leaving a surface whose entire purpose is one glance requiring a scroll to see a second provider. Raising the ceiling for everything was rejected: nothing about the activity list or the session view got taller, so they keep the height they were designed at and the resting state of the app is unchanged. The pair is mirrored in `lib/popoverHeight.ts` and pinned by tests on both sides. | If a second surface asks for the ceiling, revisit whether 780 should simply become the contract. If a supported display makes 780 unreasonable, the Usage surface loses the extra room rather than the window overflowing. |
| D-23 | **Resolved (2026-08-16): the consent-state read was dropped in the extraction, and is now re-homed** | `source-allowlist.toml`'s `local-git-and-wsl-boundary` rule instructs that the consent-state read and the protected-directory list "must be re-homed, not dropped" | Both halves. The protected-directory list arrived with the extraction as `paths::protected`; the state read did not, and its absence went unrecorded until now. The record of which directories the user granted is re-homed onto this app's own store, behind the engine's `ConsentGrants` seam — and written against that public trait rather than ported from the private sidecar, so no source-allowlist rule covers it and it carries ordinary Git/DCO provenance. | The extraction landed the pure half and left the stateful half to the embedding application, which was the right split — but nothing then implemented it, so the app passed `NoConsentGrants` and the engine's whole consent apparatus sat inert. The visible cost was a folder-permission dialog raised by a background pass at launch, with no window on screen to explain it. Re-homing onto the store rather than the sidecar is deliberate: the private sidecar is re-read per pass because a separate daemon shares it, and this app is single-process with the store already its single source of truth. | If a second process ever writes the record, revisit whether the store is still the right home. |
| D-24 | **The consent flow diverges from the reviewed private implementation in five places, one of them by declining a capability** | (Not a matrix row; records what the D-024 port deliberately does *not* copy, so the privacy claims stay auditable) | Per-directory recorded-denial handling; the three folder usage-description strings; grants in the store rather than a JSON sidecar; an ordinary refresh pass after a grant instead of a targeted rescan; and **no Full Disk Access shortcut anywhere in the interface**. | The first two are gaps in the source rather than features of it. Its recovery UI appears only when *every* visible directory is denied, so a single stuck directory hides behind a button that cannot help it. And it ships no usage-description strings, so the system dialog falls back to generic copy at exactly the moment the user is deciding whether to trust the request. Two more are architectural: the sidecar exists for a daemon this app does not have, and the targeted rescan is organization-filtered, which is meaningless without an organization. The last is a reversal worth stating plainly. The source carries a full-disk-access deep link that nothing calls, and this port first read that as an oversight and wired it. Testing the blocked state showed the premise was wrong: a recorded refusal *creates* the per-folder row in Files and Folders, so the narrow control always exists, and after a reset — the case with no row — no decision is recorded either, so asking again produces a real dialog. Every blocked state this app can reach is therefore recoverable through the per-folder toggle, which leaves a full-disk-access button as nothing but a disproportionate offer: antiburn wants three folders, that setting grants mail, messages, browsing history, and every other application's private data. Offering it directly under a notice promising to read "repository names and locations only" would trade the trust the notice exists to build for a shortcut nobody needs. The unwired link in the source now reads as restraint rather than neglect. | If a blocked state is ever found that the per-folder control cannot reach, this is the decision to revisit — with the specific state named. |
| D-25 | **The first-run flow is its own window, not a popover surface** | Onboarding is one of the popover's per-view bounded-height surfaces, at 380px wide | A standalone 680×480 non-resizable window (`src-tauri/src/onboarding.rs`, `src/views/OnboardingView.tsx`), opened automatically at launch while `onboardingCompleted` is false, with the menu-bar item routing there instead of to the popover until it is finished. The popover drops to three surfaces and no longer carries a height for a fourth. While that flag is false the app is also an ordinary macOS application rather than an accessory one — Dock icon, ⌘-Tab entry, application menu, and a ⌘Q that actually quits — dropping back to `ActivationPolicy::Accessory` the moment the flow finishes. | Two failures, both of them the popover's shape rather than the flow's content. The popover is created hidden and shown only by a click on the menu-bar item, so a fresh install booted into the menu bar and waited — silently — for the reader to notice a 16pt template glyph: the surface whose whole job is establishing trust sat behind the discovery problem it should have been solving. And 380px is the wrong room for the decisions being made in it. The Repositories step stacks a folder-permission notice — whose three buttons wrap to two rows at that width — above ~60px repository rows, leaving two or three visible at the one moment the reader is choosing what antiburn may read. 680×480 leaves that step about 130px of list under a notice that no longer wraps, and every other step more room than it had. The window follows the settings window's chrome exactly (fixed size, non-resizable, macOS overlay title bar) rather than inventing a second pattern, and shares its centring helper. The activation policy is the same argument one step further along: the accessory policy assumes the reader already knows where the app lives, and the one window whose job is to tell them cannot also depend on it. Without a Dock icon, a reader on step two who clicks another application has no route back — not the Dock, not ⌘-Tab, not Mission Control, only the menu-bar glyph they have not been told about yet. It is keyed on the flag rather than on the window being visible, so closing the window early does not take the way back with it, and `RunEvent::Reopen` makes the Dock icon reopen the flow. The one behaviour deliberately not carried over is Escape-to-dismiss: correct for a transient tray surface, wrong for a decorated window in the middle of a task, so ⌘W closes and Escape does nothing. | If the flow ever shrinks back to something a 380px column can hold, or if the matrix row is amended to describe a first-run window. Whichever happens, the popover's own three-surface height table (`lib/popoverHeight.ts`) is the thing to check first. |
| D-26 | **One notification is not gated by the notification preferences** | Every notification kind is gated twice, by the master switch and by its own | `Kind::MenuBarHome` — "antiburn lives in your menu bar" — bypasses both, exactly as `Kind::Test` does, and is forced to hang off the menu-bar item whatever the reader's placement preference says. | It fires once in the application's whole life, as the first-run window closes, and it is the only thing that says where the app just went: no Dock icon, no window, and a menu-bar mark the reader has had no reason to notice. Suppressing it for somebody who turned notifications off during onboarding would leave them with a running application and no way to find it. Like the test notification, it is the direct consequence of a button pressed a second earlier rather than something antiburn decided to say. The forced placement is the same argument: a notice answering "where is it" by appearing in the opposite corner of the screen from the answer would be worse than none, so the copy and the position point at the same glyph. The override is one-shot and consumed at reveal (`nudges::AnchorOverride`), so no later notification inherits it. | If the flow ever gains a way to be re-run, this stops being once-ever and needs a real marker rather than the onboarding-completed transition it currently rides on. |
| D-27 | **What "local" means, and what it does not** | (Not a matrix row; the definition the privacy claims rest on) | antiburn is local in one exact sense: it needs no connection to any service of antiburn's — no account, no server, no backend — and it never will. Everything it does runs on the reader's machine, as the reader. antiburn watches cloud coding agents — tools that only work connected to their providers — so it is an ordinary online application: as the reader's own agent it may make network requests, read the credential and configuration files the reader's tools wrote, call a provider's API with the reader's credentials, inspect local processes and ports, and call a locally-running agent over loopback — anything the reader could do themselves. | The boundary that matters to a reader is not *technique* — whether a socket happens to open — but *dependence and disclosure*: does the app make them rely on us, and does it hand their data to anyone who does not already hold it. antiburn answers no to both, and enforces exactly that. The one hard line, kept mechanically (`crates/antiburn-local/tests/boundary.rs`, `scripts/check-boundary.mjs`, `apps/desktop/tests/no-exfiltration.test.ts`), is that no antiburn server, third party, telemetry SDK, or reporting endpoint enters the tree; a reader's own token returned to its issuing provider is not a disclosure and is unrestricted. Genuinely risky *local* acts — deleting or modifying files, killing processes, holding a credential anywhere but memory — take explicit present intent and a recorded decision. The provenance rules in `docs/oss/` are a separate matter and untouched: they govern what source may enter the repository, not what the installed app may do. | Revisit only if antiburn ever grows a service of its own that the app needs to function — that would change the first sentence, which is the one the whole claim rests on. |

## Checked, and not deviating

Recorded so the next reader does not have to re-derive them.

- **Popover geometry.** 380px wide and non-resizable; the window still opens
  at the contract's 700px and each surface requests its own bounded height
  (`lib/popoverHeight.ts`, clamped again in `popover.rs`). Per-view bounded
  height change is itself a **Keep** row, so the table implements that row
  rather than departing from it. Two things about it *are* filed above: the
  Usage surface exceeds 700 (D-22), and the short fourth entry that used to sit
  below the contract belonged to the first-run flow, which has its own window
  now and is no longer a popover surface at all (D-25). What is left is three
  surfaces, none of them below the resting height, which a test pins.
- **The pinned popover.** The tray menu can suspend all three of the popover's
  dismissals — focus loss, Escape, and a second click of the menu-bar item —
  and nothing else about the window changes: it is created once, anchored, and
  bounded exactly as before, and the pin is in memory only, so no state
  survives a relaunch. Filed here rather than as a deviation because the matrix
  row being implemented is the popover itself; a reader-controlled suspension
  of its dismissal is an affordance on top of that row, not a departure from
  it. Worth stating because the shipped behaviour now has a mode in which
  "hides when it loses focus" is untrue, and the next reader should find that
  recorded rather than surprising. If a pin ever persists across launches, or
  ever becomes the default, that is a different decision and belongs above.
- **The private-only rows.** Authentication, accounts, organizations, upload,
  publication, and the private commercial enrichment features have no public
  control, route, cache, command, simulation, or placeholder. That is the
  matrix as written, not a deviation from it.
- **Provider Usage states.** The estimate path's `live` and `detected` states
  remain unreachable, which is what D-019 requires: a session transcript
  records what was spent, never what remains, so no amount of session evidence
  can produce an allowance. That sentence is now scoped to the estimate path on
  purpose — the *limit* path answers the other question, and it answers it by
  repeating what a provider already stated rather than by deriving anything.
  The two travel over separate commands (`get_provider_usage` and
  `get_live_usage`) precisely so the estimate payload's structural guarantee —
  no percentage, no allowance, no reset, enforced by its own test — survives
  the limit surface existing.
