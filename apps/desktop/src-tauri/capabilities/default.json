{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to every antiburn window. Deliberately minimal: the shell's own commands carry the privileged work, so the webview needs no direct filesystem, shell, or process access. Note the absence of any notification permission — the notification plugin is registered, but only Rust may post one, so what is worth interrupting a reader for is decided in src/notifications.rs rather than anywhere a view can reach.",
  "windows": ["popover", "settings"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "dialog:default",
    "opener:default",
    "updater:default"
  ]
}
