# Source Contracts

These files are synthetic contract fixtures for source-reader changes. They
contain no user content or copied transcript data.

The manifest and boundary case list record accepted versions and required
malformed, missing, duplicate, conflicting, oversized, capped, and changed-source
boundaries. SQLite
contracts are created in the integration test so no database journals are
stored in the repository.
