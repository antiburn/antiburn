# Cline Messages Contract V1 Fixtures

These synthetic files define one terminal Cline root manifest and its canonical
root and child message artifacts. Tests create the matching `sessions.db` rows.
The fixtures contain only the bounded model, metric, and tool facts that the
reader retains.
