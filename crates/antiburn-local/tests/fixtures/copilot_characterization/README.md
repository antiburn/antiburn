# Copilot CLI V1 Fixtures

These synthetic event chains define the accepted Copilot CLI v1 persisted-event
contract. They test the required start, ordered event parents, terminal shutdown,
model usage, and subagent lifecycle. They contain no prompt, message, tool input,
or tool result data.
